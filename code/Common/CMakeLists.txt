fips_add_subdirectory(lua-5.1.3)
# fips_add_subdirectory(luabind)

fips_begin_module(Misc)
    fips_src(2D)
    fips_src(Debug)
    fips_src(FSM)
    fips_src(fuzzy)
    fips_src(Game)
    fips_src(Goals)
    fips_src(Graph)
    fips_src(Messaging)
    fips_src(misc)
    fips_src(script)
    fips_src(Time)
    fips_src(Triggers)
fips_end_module()